up_1: 1
form:
  text_area_rows: 8
department:
  validates:
    name:
      max: 30
    description:
      max: 5000
dish_types:
  format_date: "%d/%m/%Y"
email_regex: !ruby/regexp /\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i
admin_customer:
  per_page: 5
user:
  name:
    max_length: 50
  email:
    max_length: 200
  address:
    max_length: 255
  phone:
    min_length: 10
    max_length: 12
  password:
    min_length: 6
dishes:
  per_page: 30
