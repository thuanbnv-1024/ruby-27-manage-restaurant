<%= link_to "#{t('dinner_tables.show.order') + ' (' + current_order.order_items.size.to_s + ' ' + t('dinner_tables.show.dish')} : #{number_to_currency(current_order.send(:subtotal) ? current_order.send(:subtotal) : 0)})", cart_path, class: "btn btn-link" %>
